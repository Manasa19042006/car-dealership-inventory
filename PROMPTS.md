

## Prompt 1 — Project Initialization

```
I am building a full-stack Car Dealership Inventory System for a TDD-focused software engineering assignment.
Use this technology stack:
Backend: Node.js, TypeScript, Express.js
Frontend: React, TypeScript, Tailwind CSS
Testing will be added in a later step using Jest and Supertest.
For this step ONLY, initialize the project. Do not implement any business functionality.

```

---

## Prompt 2 — Database Layer (Prisma + PostgreSQL)

```
Continue the Car Dealership Inventory System from Step 1.
Step 1 is already completed: Node.js + TypeScript + Express backend, React + TypeScript + Tailwind frontend, GET /api/health, Git repository initialized.
For this step ONLY, implement the database layer.
Use: PostgreSQL, Prisma ORM, TypeScript
```

---

## Prompt 3 — Testing Setup

```
Continue the Car Dealership Inventory System from Step 2.
So far I have completed: Node.js + TypeScript + Express backend, React + TypeScript + Tailwind frontend, PostgreSQL database, Prisma ORM, User and Vehicle Prisma models, Prisma migration, Basic backend health endpoint.
Now I want to set up the backend testing environment before I start implementing the actual features.
Use Jest and Supertest.
```

---

## Prompt 4 — User Registration (TDD)

```
Continue the Car Dealership Inventory System from Step 3.
I want to implement USER REGISTRATION now using proper Test-Driven Development.
The endpoint is: POST /api/auth/register
```

---

## Prompt 5 — Login + JWT (TDD)

```
Continue the Car Dealership Inventory System from the current project state.
Authentication, registration, login, JWT, Prisma, PostgreSQL, Jest and Supertest are already implemented.
Now implement USER LOGIN and JWT authentication using proper Test-Driven Development.
```

---

## Prompt 6 — Vehicle Management API (TDD)

```
Continue the Car Dealership Inventory System from the current project state.
Registration, login, JWT authentication are already implemented.
Now implement the VEHICLE MANAGEMENT API using strict TDD.
Endpoints: POST /api/vehicles, GET /api/vehicles, GET /api/vehicles/search, PUT /api/vehicles/:id, DELETE /api/vehicles/:id
```

---

## Prompt 7 — Purchase and Restock (TDD)

```
Continue the Car Dealership Inventory System from the current project state.
Registration, login, JWT authentication, vehicle CRUD and vehicle search are already implemented and tested.
Now implement vehicle PURCHASE and RESTOCK using strict TDD.
Endpoints: POST /api/vehicles/:id/purchase, POST /api/vehicles/:id/restock
```

---

## Prompt 8 — Frontend Authentication UI

```
Continue the Car Dealership Inventory System.
The backend is now complete for: registration, login, JWT authentication, vehicle CRUD, vehicle search, purchase, restock.
Now implement the FRONTEND AUTHENTICATION UI using React, TypeScript and Tailwind CSS.
```

---

## Prompt 9 — Vehicle Dashboard + Admin UI

```
Continue the Car Dealership Inventory System.
Frontend authentication is already implemented.
Now build the main vehicle dashboard and admin functionality using React, TypeScript and Tailwind CSS.
```

---

## Prompt 10 — Search Fix + UI Polish

```
check everything and make it a good website also check the search button is not working
```


## Prompt 11 — Final Documentation

```
Continue the Car Dealership Inventory System from the current project state.
The complete application is now implemented.
I want to prepare the project for final submission according to the assignment requirements.

```

---

